# 連勝シミュレーター — 仕様書 (改訂版)

> Sugar Cage IP / タップ連打ハイパーインフレ・バカゲー
> 単一 `index.html` (CSS・JS 内包)。外部画像アセットなし。
> このファイルを Claude Code に渡してビルドする。実装前に「原因/方針/影響 → GO待ち → 実装」プロトコルに従うこと。

---

## 0. このゲームの芯 (最重要・読み違い防止)

- **負けは存在しない。** 数字が増え続けるのを眺める気持ちよさが全て。
- プレイヤーの唯一の入力は「タップ」。降りる判断も運試しもない。
- **タップをやめた瞬間 = シーズン終了。** これは「敗北」ではなく「最終成績の確定」。絶望・没収の演出は使わない。花道として見せる。
- レートは無限に増え、そのうち画面から溢れて読めなくなる。それでいい。
- 連勝数は 6 桁まで壊れないこと(実質無限)。

この 5 点に反する実装・演出は却下。

---

## 1. 状態遷移

```
[タイトル] --キャラ選択--> [シーズン開始]
   ↑                              │
   │                              ▼
[最終成績] <--3秒無達成-- [バトル] --目標達成(WIN)--> [レートアップ] --自動--> [バトル] ...
```

- **State 0: タイトル** — 最高記録表示 + キャラ選択(ラコ / オウ)。選んだ体で 1 シーズン。
- **State 1: バトル** — 3 秒の押し合い。目標タップ数に到達で WIN → State 2。到達せず 3 秒経過(0 タップ含む)で → State 3。
- **State 2: レートアップ** — WIN 表示 + レート上昇のカウントアップ演出。演出後、自動で次の State 1 へ(シーズンが途切れず連続進行)。
- **State 3: 最終成績** — 到達しなかった時点でシーズン終了。最終成績 + 偽・全国ランキング + 共有カード。→ タイトルへ。

**重要:** State 3 でレートを 0 にしない。その時点の連勝数・レートがそのまま最終成績になる。新シーズンは State 0 から連勝数 0 でリスタート。

---

## 2. 数字の扱い (このゲームの技術的な芯)

### 2.1 原則: レートを実数として保存しない

レートは連勝数の完全な関数:

```
連勝 1, 2  → レート 10 (固定)
連勝 n≥2   → レート = 10 × 2^(n-2)   (= 前回×2)
例) 10 → 10 → 20 → 40 → 80 → 160 → …
```

**状態として持つのは連勝数 (整数) だけ。** 巨大なレート実数は保持しない。

- 普通の数値型で ×2 を続けると **連勝 ~1024 で Infinity に落ちて壊れる**。指数ベースで扱えば発生しない。
- したがって **BigInt は不要。** 連勝数は通常の整数で 6 桁でも余裕。

### 2.2 表示は「読める帯」と「彼方の帯」の 2 段

レートの桁数(常用対数)は連勝数から直接出る:

```
log10(rate) ≈ 1 + (連勝数 - 2) × 0.30103   (連勝 n≥2)
```

**帯 A: 読める帯 (連勝 0 〜 ~238) = 本編**
ここが実プレイの主戦場。日本語単位(万〜無量大数)で読める。カウントアップ演出を全力で気持ちよく。

- **連勝 ~51 以下:** `rate = 10 * 2^(streak-2)` を通常数値で厳密計算(この範囲は整数として厳密)。→ 10, 20, 40, 80… の桁ローリングを崩さない。ここが快感の核。
- **連勝 ~52 〜 ~238:** 厳密計算は MAX_SAFE_INTEGER を超える。§2.3 の指数→仮数変換で `X.XX 単位` を組み立てる(下位桁の丸めは許容、誰も見ない)。

**帯 B: 彼方の帯 (連勝 ~238 超) = 保険**
日本語単位が尽きる領域。作り込む場所ではない。壊れず終わらせるためのフォールバック。CONFIG で切替:
- `exponential`: `1.24 × 10^71589` のような指数表記。
- `meme`: 「無量大数の彼方」「計測不能」等のネタ表示、または数字が画面外に溢れる演出。

> **Gemini が確実に落とすポイント:** 単位配列が無量大数で尽きて `undefined` を吐く。帯 B のフォールバックを必ず実装すること。

### 2.3 単位フォーマッタ (帯 A 後半用の指数→表示変換)

巨大数を保持せずに `X.XX 単位` を作る:

```
exponent   = 1 + (streak - 2) * 0.30103   // log10(rate)
E          = floor(exponent)
frac       = exponent - E
mantissa   = 10^frac                        // 1.0 〜 9.99
unitIndex  = floor(E / 4)                    // 万=1, 億=2, ...
remainder  = E - unitIndex * 4
displayNum = mantissa * 10^remainder         // 1.00 〜 9999.xx
=> `${displayNum.toFixed(2)} ${UNITS[unitIndex]}`
```

`UNITS[unitIndex]` が undefined(= 帯 B 突入)なら §2.2 帯 B へ分岐。

**注意 (後半のジュース設計):** 帯 A 後半〜帯 B では、レートの数字は指数が 0.3 ずつ増えるだけで見た目が動きにくい。**後半の気持ちよさは数字ではなく、連勝カウンタ・画面 FX・音で持たせる。** べにの「そのうち見えなくなる」割り切りと整合。

---

## 3. モード差分 (ラコ / オウ) — 唯一の実装差分は目標タップ数関数

同じインフレを別の体で味わう。差分は `targetTaps(streak)` だけ。

### オウ = 低め固定「ご褒美だけ浴びる純粋機械」
- 目標タップ数を低い定数に固定(既定 1)。CONFIG で 1〜3 に調整可。
- シーズンは実質、手を止めるまで終わらない。純粋な報酬マシン。

### ラコ = 緩く上げる「ほんの少し身体が乗る」
- `1〜10連勝: 1回`
- `11〜50連勝: floor(streak / 5)`
- `51連勝〜: 緩やかに増加、ただし上限 21回 (=7回/秒×3秒)。絶対に不可能にしない。`
- 高連勝で手が滑ると成績確定するが、それは「敗北」ではなく「そこがラコの限界だった」という最終成績。END 画面は死ではなく花道であること(§0)。

キャラ性: **オウ=淡々と無限。ラコ=乗ってくる身体感覚。** この対比が 2 モードの魂。演出テキストもこの性格差に寄せる(§5)。

---

## 4. バトル / タイマー

- 制限時間 3 秒(CONFIG)。
- **計時起点は「最初の 1 タップ」。** State 1 遷移直後ではない。WIN 演出の余韻中に時計が回る理不尽を防ぐ。
- 3 秒内に目標タップ到達 → 即 WIN。
- 3 秒経過して未到達(0 タップ含む) → シーズン終了(State 3)。実質「タップをやめた/手が止まった」の検出。
- UI: 中央に大きく「押せ押せ押せ!!」等。タップごとに軽い画面シェイク(`transform` のみ、layout を触らない)。

---

## 5. 演出 (バカゲーの快感の 8 割)

### レートアップ演出
- 「◯連勝!!」をデカデカ表示 + 加算レートのカウントアップが現在レートに吸収される。
- 連勝が伸びるほど背景が派手に(青→赤→金→宇宙柄、CSS グラデ)。
- **演出時間は log(rate)=連勝数 に比例、上限でキャップ。** 序盤サクサク、伸びるほど"ため"。CONFIG で base + 1桁あたり増分 + 上限。

### 音 (Web Audio、画像不要・体感が跳ねる)
- タップ / WIN ごとにピッチを半音ずつ上げる(連勝で上昇感)。
- シーズン終了で解決音、または間抜けに崩れる音(ネタ)。

### ハプティクス
- `navigator.vibrate()`: タップ=短振動、シーズン終了=長め。モバイルのみ。

### キャラ実況 (フレームとして薄く乗せる)
- バトル中の煽り、WIN 時の茶々、シーズン終了時のオチ台詞。
- オウ=淡々/無感情寄り、ラコ=テンション高め、で差をつける。
- 台詞は CONFIG の配列で差し替え可能に(後から濃くできる)。
- (任意・後回し可) 倒す相手を 32 人ロスターのカメオで差し替え。絵は絵文字/名前で十分。

---

## 6. 最終成績 = 偽・全国ランキング (しゅがけ風刺の核)

シーズン終了で、連勝数から決定的に算出する **偽の全国ランキング** を表示:

```
2026 SEASON 最終成績
使用キャラ  ラコ
最高連勝    347
最終レート  無量大級
全国 4,182,097 位 / 参加者 4,182,103 人
```

- **どう頑張っても下位** になる算出にする(下から数えた方が早い)。
  - 例: `participants = base + f(streak)`、`rank = participants - (streak % 7) - 1`。連勝が伸びても順位はほぼ最下位、が笑いどころ。CONFIG で調整。
- 「無意味な数字を、無意味な尺度で順位付けする」構図そのものが Sugar Cage Tube の注目経済の風刺と地続き。
- **共有カード**(べに恒例の Skin 系)を 1 枚生成。「私は◯連勝・◯垓で散った」を画像化 = クソ実績を自慢する満足感の風刺として完成。実装は既存の OGP/共有カード手法を流用。

---

## 7. 実装の地雷 (Gemini が落とす / 必ず対応)

- **`touch-action: manipulation`** — 連打中のダブルタップズームを殺す。
- **`user-select: none` (+ `-webkit-user-select`)** — 連打中のテキスト選択・長押しメニューを殺す。
- `touchstart` と `mousedown` を両バインド(スマホ+PC 連打対応)。※二重発火は「速く勝つ」だけで害なしなので dedup 不要。
- 画面 FX は `transform` / `opacity` のみ(GPU 合成)。layout を触るプロパティを毎フレーム触らない(低スペック機のジャンク防止)。
- **単位配列が尽きた時の帯 B フォールバック** (§2.2)。
- **localStorage 注意:** claude.ai アーティファクトのプレビュー内では動かない → 最高記録が消える。実運用(GitHub Pages 等)では動く。動作確認段階でのみ織り込む。

---

## 8. レイアウト

- レスポンシブ。中央揃え。数字の大きさと勢いが最も目立つデザイン。余計なメニュー不要。
- タイトル画面: 最高記録 + ラコ/オウ の 2 択のみ。

---

## 9. CONFIG (ファイル先頭、ライブチューニング用)

```js
const CONFIG = {
  battleDurationMs: 3000,
  rateBase: 10,          // 連勝1,2のレート
  rateMultiplier: 2,     // 3連勝目以降 ×2
  exactStreakMax: 51,    // ここまで厳密整数計算(桁ローリング)

  modes: {
    ou:  { name: "オウ", targetTaps: () => CONFIG.ouFixedTaps },
    rako:{ name: "ラコ", targetTaps: (s) =>
             s <= 10 ? 1 :
             s <= 50 ? Math.floor(s / 5) :
             Math.min(21, /* 緩い増加式 */ Math.floor(s / 5)) }
  },
  ouFixedTaps: 1,        // 1〜3で調整

  fx: {
    baseMs: 600,         // レートアップ演出の基準時間
    perDigitMs: 40,      // 1桁増えるごとの加算
    capMs: 2500          // 上限
  },

  audio:  { enabled: true, basePitch: 220, semitoneStep: 1 },
  haptics:{ enabled: true, tapMs: 10, endMs: 200 },

  units: ["", "万","億","兆","京","垓","秭","穣","溝","澗",
          "正","載","極","恒河沙","阿僧祇","那由他","不可思議","無量大数"],
  beyondMode: "exponential", // "exponential" | "meme"

  ranking: { participantsBase: 4180000, /* 常に最下位付近になる算出 */ }
};
```

---

## 10. ビルド順序 (手触り優先)

1. State 0/1/2 のループ骨格 + オウ(固定1タップ)で「WIN→レートアップ」の連続が気持ちいいか検証。**ここが面白くなければ他は無意味。**
2. 数字表示: 厳密整数(帯 A 前半)の桁ローリングを詰める。
3. ラコの目標タップ関数 + キャラ選択画面。
4. 帯 A 後半(単位フォーマッタ)+ 帯 B フォールバック。
5. 音・ハプティクス・演出時間スケール。
6. State 3 最終成績 + 偽ランキング + 共有カード。
7. キャラ実況テキスト。

各ステップ、実装前に「原因/方針/影響」を説明して GO を待つ。
